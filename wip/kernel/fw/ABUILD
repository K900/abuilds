# ABUILD generated by mkpkg_generator.sh
pkgname=kernel-firmware
pkgver=9999
pkgbuild=1
arch=("fw")

shortdesc=("Firmwares for the Linux kernel")

tags=("sys-kernel base")

source=("git:git://git.kernel.org/pub/scm/linux/kernel/git/dwmw2/linux-firmware.git")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware
	cp -rv * ${pkgdir}/lib/firmware/
}
